server:
  port: 8080    # 后端启动端口，前端 vite.config.js 里代理的目标端口要一致

spring:
  application:
    name: vue-rabbit-server    # 应用名，日志里会显示

jwt:
  secret: rabbit-shop-jwt-secret-key-2024-very-long-string-for-hs256-algorithm
  expire: 604800000